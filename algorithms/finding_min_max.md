Finding minimum or maximum of two numbers

```js
// Finding lowest of two values
x1 > x2 ?
  x2 :
  x1;
  
// Finding highest of two values
x1 > x2 ?
  x1 :
  x2;
```

Simpler way:

```js
Math.min(x1, x2);
Math.max(x1, x2);
```
